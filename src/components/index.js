import App from './App';
import SignIn from './SignIn';
import Slack from './Slack';
import Sidebar from './Sidebar';
import MainContainer from './MainContainer';

export {
  App, //when the value of key and key are same exported from the app.js file inside the components
  SignIn,
  Sidebar,
  Slack,
  MainContainer
};
